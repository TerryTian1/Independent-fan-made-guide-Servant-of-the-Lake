# Servant of the Lake 本地版实施范围

## 当前目标

使用项目中已准备的 `homepage.json`、`keywords.json`、`关键词.md`、`关键词素材.md` 和 `favicon_io/`，实现一个能在本地浏览器打开的 Servant of the Lake 英文攻略站。

## 本轮必须完成

1. 在 `site/` 中建立 Next.js + TypeScript 静态站。
2. 完成 9 个核心页面：首页、Walkthrough、Day 1、Day 2、Day 3、Demo & Lite、Achievements、Puzzles、Secrets & Endings。
3. 顶部只保留 5 个唯一入口：Home、Walkthrough、Achievements、Puzzles、Secrets & Endings。
4. 使用现有 19 个关键词分配结果，不再临时扩张页面数。
5. 使用已准备的 Favicon，完成基础 title、description、canonical、sitemap 和 robots。
6. 完成桌面端和移动端的本地浏览器验收，确保导航、内链、谜题折叠和剧透控制可用。
7. 在 `site/README.md` 说明关键文件、本地启动命令、构建命令和后续 Cloudflare 配置。

## 本轮明确不做

- 不创建或推送远程仓库。
- 不登录、连接或部署 Cloudflare。
- 不使用 Vercel。
- 不加入数据库、登录、评论、广告、GA4、Plausible 或 Clarity。
- 不为次要功能引入 CMS、服务端 API 或复杂部署适配层。

## Cloudflare 预留方式

站点使用 Next.js Static Export，构建产物在 `site/out/`。后续获得部署批准后，Cloudflare Pages 可使用：

- Root Directory：`site`
- Framework preset：`Next.js (Static HTML Export)`
- Build command：`pnpm build`
- Build output directory：`out`

本轮以本地 `pnpm dev` 和静态产物预览为交付边界。
